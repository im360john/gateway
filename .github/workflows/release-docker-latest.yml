name: Auto build and publish of gateway image

on:
  push:
    branches:
      - 'main'

jobs:
  trigger-build-latest:
    permissions:
      contents: write
      packages: write
    uses: ./.github/workflows/release.yml
    with:
      version: latest
      extra_args: '--snapshot'

